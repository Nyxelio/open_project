<div class="row expanded">
  <div class=" columns">
    <h1><%=	t(:new_category, scope: 'actions') %></h1>

    <%= render 'form', action: t(:create, scope: 'actions') %>

    <%= link_to t(:back, scope: 'actions'), families_path, class: 'button secondary' %>
  </div>
</div>
