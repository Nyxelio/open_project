<p id="notice"><%= notice %></p>

<h1 class="title"><%= t(:list_projects, scope: 'actions') %></h1>


<div>
  <table>
    <thead>
    <tr>
      <th><%= t(:name, scope: 'actions') %></th>
      <th><%= t(:is_close, scope: 'actions') %></th>
      <th><%= t(:estimated_start_at, scope: 'actions') %></th>
      <th><%= t(:estimated_end_at, scope: 'actions') %></th>
      <th><%= t(:estimated_duration, scope: 'actions') %></th>
      <th><%= t(:real_start_at, scope: 'actions') %></th>
      <th><%= t(:real_end_at, scope: 'actions') %></th>
      <th><%= t(:real_duration, scope: 'actions') %></th>
      <th><%= t(:difference_hour, scope: 'actions') %></th>
      <th colspan="1"></th>
    </tr>
    </thead>

    <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= project.name %></td>
        <td><%= project.is_close %></td>
        <td><%= project.estimated_start_at %></td>
        <td><%= project.estimated_end_at %></td>
        <td><%= project.estimated_duration %></td>
        <td><%= project.real_start_at %></td>
        <td><%= project.real_end_at %></td>
        <td><%= project.real_duration %></td>
        <td><%= project.difference_hour %></td>
        <td><%= link_to t(:show, scope: 'actions'), project %> <%= link_to "<i class='fi-pencil'></i>".html_safe, edit_project_path(project) %> <%= link_to "<i class='fi-x'></i>".html_safe, project, method: :delete, data: { confirm: t(:confirm, scope: 'actions') } %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>

<div class="actions">
  <%= link_to t(:new_project, scope: 'actions'), new_project_path, class: "button newProject" %>
</div>

<div class="row small-8 small-centered large-centered">
  <% @projects.each do |project| %>
      <div class="columns project-container">
        <div class="callout">
          <h5><%= link_to project.name, project_path(project)%></h5>
        </div>
      </div>
  <% end %>
</div>

